# --- Dependencies ---
node_modules

# --- Environment ---
.env
.env.local
.env.*.local

# --- Build ---
dist
dist-ssr
*.local
# Stale build output accidentally committed at the repo root (2024-era)
isolate

# --- Convex generated code ---
# NOTE: src/convex/_generated is COMMITTED on purpose (Convex best practice):
# it makes typechecking work in CI and code browsing work on GitHub, without
# requiring a deployment. It is regenerated by `convex dev` — never edit by hand.

# --- Logs ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*

# --- Tests ---
coverage

# --- Editor / OS ---
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
